# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (contains secrets)
.env
.env.local
.env.*.local

# Kiro settings (contains API keys and tokens)
.kiro/settings/

# Kiro hook state files (auto-generated, not committed)
.kiro/hooks/.dependency-state.json
.kiro/hooks/.security-alerts.json

# Note: .kiro/ directory is intentionally NOT ignored
# It must be committed for hackathon submission requirements
# Only specific state files within .kiro/hooks/ are ignored

# Vercel CLI cache
.vercel

# Netlify CLI cache
.netlify

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
