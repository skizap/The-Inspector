# -----------------------------------------------------------------------------
# AI Provider Configuration
# -----------------------------------------------------------------------------

# AI Provider Selection (Client-Side)
# Determines which AI service to use for package analysis.
# Values: "openai" or "openrouter"
# Default: "openai" (if OPENAI_API_KEY is set and this variable is unset)
# 
# Recommended: Set explicitly to "openrouter" for access to diverse models
# including reasoning models like Kimi K2 Thinking.
VITE_AI_PROVIDER=openrouter

# -----------------------------------------------------------------------------
# OpenAI Configuration (if VITE_AI_PROVIDER is "openai")
# -----------------------------------------------------------------------------

# OpenAI API Key (Server-Side Only)
# Required when VITE_AI_PROVIDER is "openai"
# DO NOT prefix with VITE_ as that would expose it to the browser (security risk)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# -----------------------------------------------------------------------------
# OpenRouter Configuration (if VITE_AI_PROVIDER is "openrouter")
# -----------------------------------------------------------------------------

# OpenRouter API Key (Server-Side Only)
# Required when VITE_AI_PROVIDER is "openrouter"
# DO NOT prefix with VITE_ as that would expose it to the browser (security risk)
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Default Model Selection (Client-Side)
# Sets the initial model in the UI dropdown
# Examples:
# - OpenRouter: "moonshotai/kimi-k2-thinking", "anthropic/claude-3.5-sonnet", "openai/gpt-4o"
# - OpenAI: "gpt-4o", "gpt-4-turbo"
# Users can override this via the UI model selector
VITE_DEFAULT_MODEL=moonshotai/kimi-k2-thinking

# -----------------------------------------------------------------------------
# OpenRouter App Attribution (Optional)
# -----------------------------------------------------------------------------

# Site URL and Name for OpenRouter app listing
# Optional but recommended for OpenRouter deployments
# Enables app listing at: https://openrouter.ai/apps
VITE_SITE_URL=https://your-app.vercel.app
VITE_SITE_NAME=The Inspector
